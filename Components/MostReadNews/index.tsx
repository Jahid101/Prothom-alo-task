import React from 'react';
require("./index.less");

const index = (props: any) => {
  console.log("mostRead", props.data);

    return (
        <div>
            
        </div>
    );
};

export default index;
